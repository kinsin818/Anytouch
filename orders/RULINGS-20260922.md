# 主窗裁决记录 2026-09-22：对二审 4×P0 + 6×P1

裁决人：Qoder（施工总窗/主写）。依据：四轮冻结共识 + 总军令 + 本轮实测。
被审文件：`Anytouch-最终综合立项方案-Doubao汇总.md`；初审：WorkBuddy；二审：DuMate。

## P0 裁决（4 条：3 采纳 1 部分采纳）

| # | 二审指控 | 裁决 | 处置（落点） |
|---|---|---|---|
| P0-1 门票被跳过 | **采纳**。总军令"直接开写"越权。但注意：被审文件是 Doubao 汇总版，门票共识在四轮文档里，汇总丢了这层不算笔误算翻案 | 施工口径即刻生效：**S0 与 S1 框架继续（T1 已完成，S0/S1 不依赖未过门票）；S2 录制实测、S3 兜底、S4 上线 以 T2/T3/T-US 门票通过为前置门**。T2/T3/T-US 需老板配给（模型 Key、真机），已由主窗列入待派工单 | `orders/ANYTOUCH-S1-ORDER.md` 头部写门票依赖表 |
| P0-2 商业模式翻案 | **采纳**。四轮冻结共识（WorkBuddy R-W1 / DuMate R2 / Marvis 修正3 / Qoder F2）：免费版=限创作不限执行（3 任务/10 步）、**导出常开+默认脱敏、录制是第一入口**。把录制锁付费墙与共识直接冲突且未声明变更 | 施工按冻结共识执行：付费解锁=无限任务/步数+模板库+快车道修复；录制与导出在免费版。此条若老板要改，是**决策变更**需老板明示，不属主写权限 | 写入本文件为默认口径；S4 定价文案军令引用冻结共识版 |
| P0-3 MediaProjection 消失 | **采纳**（与初审硬伤 1 同源，双审同指必改）。S1 验收在模拟器**保留**——因 S1 主链是节点树定位，不依赖截图授权；授权炸弹全部收进 S3 一门门票 | T3 判官项单列：Android 14+ 每会话授权对无人值守任务的打断实测 + 授权会话常驻设计 + 失败降级纯节点树；S3 开工前置=T3 该项通过。WorkBuddy 补法（首启授权+被杀重引导+降级不卡死）全盘采纳进 S3 军令 | `ANYTOUCH-S3-ORDER.md` 待写，含 R0-3 落点与验收 |
| P0-4 游戏签到未过预检 + S4 丢"零模型"口径 | **部分采纳**。按 Marvis 预检 6 问第 6 条（目标 App ToS 灰区不确定）：游戏自动签到多数 ToS 明令禁止 → 不"拒"但**降级：仅用户自建、不预置**；第三个预置位改判给 Marvis 预检通过的 Discord 单账号签到（待其 ToS 复核一句话）。"执行期零模型调用+任务级成功率分母"确系 S4 验收漏项，采纳补回 | 首发模板清单改：Gmail 清理 / 相册清理 / Discord 签到（预置）；游戏签到→用户自建白名单外模板，不进预置库 | S4 军令模板清单节 |

## P1 裁决（6 条：5 采纳 1 已实测关闭）

| # | 议题 | 裁决 |
|---|---|---|
| 许可证/F-Droid 互斥 | **采纳定案**：执行器+DSL **Apache-2.0**（Qoder 第四轮提案，Marvis AGPL 案不采——商用传染吓退集成且法务成本高的风险未偿还）。F-Droid 只收 100% FOSS 的 lite-executor flavor；闭源编译层/录制器不入该 flavor。两渠道互斥问题以 flavor 切分消解 |
| W 支线消失 | **采纳**：Windows 左舵付费先行验证器恢复为**并行 W 轨**，不占安卓排期、老板拍板人力后再开；主窗在总排期表中列 W0 |
| NL 实验开关 | **采纳**：NL→步骤为实验开关默认关，T2 ≥80% 才升门面。与 P0-1 门票联动 |
| "今天出 demo" | **采纳收敛**：改为"今天可出 S0 契约层可编译 demo"。本文件落盘时 S0 已在跑（STAGE-01 已验收 13/13 绿），措辞按事实修正 |
| 总工期/人力 | **部分采纳**：T1 已完成，主窗可给排期：S0 1 周（进行中）→S1 3 周→S2 2 周→S3 2 周→S4 2 周，串行 10 周、双轨并行 8 周（W 轨独立）。人力数字是老板拍板项，主窗只报需求：施工期 1 人够，测试期需要 0.5 人跑 T 票 |
| Python 兼容性 | **实测关闭**。本机事实：默认 3.14.7，**已并存 3.12**（`py -3.12` 直接可用）。"3.13.13"为错误陈述；"需侧装"为多余动作。T 票跑批统一 `py -3.12` 建 venv |

## 施工状态快照（裁决生效时点）

- S0：骨架 commit `de3d6b4`；STAGE-01 契约移植已验收（独立复跑 13/13 绿）commit `fec140a`；STAGE-02 Mock闭环+CI 施工中
- 门禁：S1 可开工（T1 已完）；S2/S3/S4 各自门票未过不开（P0-1/P0-3 生效）

## T3 真机三案裁决（老板直接拍板，2026-09-22 13:40 UTC）

| # | 议题 | 裁决 | 落点 |
|---|---|---|---|
| T3-R1 | MIUI 搜索框 SET_TEXT/PASTE 双拒，是否加 IME 代理第三通道 | **不加**。新攻击面太大；边缘场景由用户录制时手动输入规避；输入法劫持属高危面，第一版不碰 | 产品代码冻结此路径；如海外真机批量复现再重议 |
| T3-R2 | MIUI 首页容器拒 ACTION_SCROLL_FORWARD，是否允许坐标手势兜底 | **红线不破，不上坐标手势**。魔改 ROM 的锅写进使用文档："MIUI 首页先手动滑一下再录" | `docs/usage-notes.md` 已知限制节 |
| T3-R3 | 中文界面无 "password" 词，C5 高危链真机轮怎么测 | **第一版不测中文语境**。目标用户=海外原生安卓；高危链切英文/国际版真机测，符合海外优先 | 真机 C5 轮以英文界面为前置；本机（国行中文）不强测 |

## T3-R4 成功率口径变更（老板明示，2026-09-22 14:05 UTC）
- 原口径（S0/S1 冻结共识）："真机 ≥95%" 属 T3 门票。
- **老板裁决改为**：第一版 = **既有真机（K40，后续 +K80）+ AVD 三档矩阵（API 31/34/35 Pixel 镜像）验证通过**；不花钱采购新真机；第一版上线有收入后再议二手国际版补样本。
- 主窗备案两点诚实边界：①AVD 结构上覆盖不了厂商 ROM 雷（雷 12/13 类）与真实电源管理（doze/SmartPower），矩阵绿≠全 ROM 绿；②Samsung/Moto One 海外大头样本缺口显式记入已知限制，写进 S4 上线文案的风险段，不虚标。
- 原"真机≥95%"表述在冻结报告（docs/ANYTOUCH-S0/S1-final-report.md）中不改原文，以本节为最新口径。

## S2 实测层四案裁决（老板拍板，2026-09-23 14:3x 本地）

| # | 议题 | 裁决 | 落点 |
|---|---|---|---|
| S2-R1 | L1/L2/红线 E/§4 判据出自会话粘贴、磁盘无原文，独立验收方判"虚靶"（DEVIATIONS A1/D-2/D-3） | **生效派单落盘**：授权主窗照录登记为 `orders/ANYTOUCH-S2-ondevice-ORDER.md`。主窗如实标注其性质=**条款级复述**（逐字原文已不可复现），每条带可证伪状态与证据指针，与老板记忆不符处以老板更正为准 | 该文件 §0 性质声明 + §7"后续一律引 ORDER 不引 DRAFT/会话"；DEVIATIONS D-2/D-3 结案 |
| S2-R2 | §4"设备端 JVM 单测 ≥270"按真数 176 差 94，补测形态待定（DEVIATIONS D-4） | **不凑数、第一版不卡此阈值**。缺口以后补采集桥/浮层/门禁这些**平台相关**用例来还，不进第一版门禁 | 记入 ORDER §6 判据表该行；平台测试补票挂后续批次待办，禁止恒真用例刷分母 |
| S2-R3 | S4 装机引导话术是否与主活同批 | **单独 commit 获认可**，不与主活混批（主活先收口、次单后排） | 现状即如此：`b4afb1f`（主活=`05b37d9`） |
| S2-R4 | 雷 15/16/17/18 的小米系真机复验是否卡主流程 | **延后**，等真机有空再测，不卡主流程；模拟器口径不外推的边界继续在证据里写明 | `evidence/S2/s2-ondevice-record-replay.md` §7 诚实清单保留；T3 待办不占主活排期 |

**下一项派工（老板同批明示）**：开**录制 UI 界面**（含步骤列表编辑页：删步/改名/移序，调 STAGE-21 冻结纯函数）。属主窗窄口，非 worker 范围（DRAFT §主窗窄口已钉死"service 采集钩子 + 悬浮球录制开关 UI + 步骤列表编辑页"不派工）。

## S3 批前三裁（老板 2026-09-23 第三批，原文照录）

| # | 议题 | 裁决（原文） | 落点 |
|---|---|---|---|
| S3-R1 | 平台层 94 例 JVM 缺口经全量审计后**只有约 27–30 例真实判据可锁**，S2-R2 的"≥270"怎么记账 | **(a) 按真实判据数记账，不设 270 硬阈值**——"那些UI窗口调用、没被调用的Runner本来就写不出有意义的测试，为凑数写恒真断言没用，27-30例真实判据锁死就行，后面真有回归再补" | 任务 #38 范围改为审计点名的 A1–A5（假边重派、awaitLanded 反向半边、采集层两判据）优先、B 层次之；METRICS 只报真数；已抓出的 2 例恒真断言登记不追溯改（STAGE-22 已验收） |
| S3-R2 | 示例语料与 UI 占位文案用海外 App 还是开工令口语里的"美团" | **同意**——"示例语料全用海外App，不用美团，本来就是海外专供，没问题" | `orders/ANYTOUCH-S3-byok-ORDER.md` §4-1 由"备案待裁"转"已裁生效" |
| S3-R3 | 切片 A 之后的排期 | **做切片 B**——"继续往下做切片B：设备侧HTTP传输、Key加密存储、失败话术这些" | 本批交付即其落盘，见 `evidence/S3/slice-b-key-surface.md` 与 ORDER §5.1 B 行 |

## S31 批内主窗自裁一条（worker 显式留给主窗裁，可被老板一句话覆盖，2026-09-24）

| # | 议题 | 裁决 | 依据与代价 |
|---|---|---|---|
| S31-A1 | STAGE-31-A worker 自述 §6-4：`clueTreeOf` 在预算触顶时，队列里剩余的 `getChild` 副本不 `recycleQuietly`（搬移前后同型，worker 未顺手改，请主窗裁） | **不改**，登记为已知残留 | 依据三条磁盘事实：① 与搬移前**同型**（旧 `descendantClue` 也是 `break` 出队、队列余货不回收），改了即破坏 §5-D7 反向验证所依赖的"遍历形状逐字相同"这一验收判据；② 上限有界（`DESCENDANT_BUDGET=32` / `DEPTH=8`，`CaptureBridge.kt:684-685`），最坏一次点击残留 ≤32 个未回收副本，且**已出队**的副本逐只回收（`:586`）；③ 归档证据里这条路径从未走到：`evidence/` 全档扫过，`S2SMOKE clue=path` 设备日志行共 **47 条**，带 `truncated(预算内未遍历完)` 的 **0 条**（唯一出现该串的文件是 worker 自述里的源码引用，不是设备读数）⇒ 冷路径，不是"每天在漏"。代价照实：a) 触顶即"连歧不歧义都没看清"，判据侧已按 `CluePick.Rejected(truncated=true)` 弃用整条并留痕，所以残留不污染结论；b) `recycle()` 在现代运行时是否仍有实际作用，本机**无可验证据**（`android.jar` 是 stub，`compileSdk=36`），故不拿"它是空操作"当不改的理由——若将来设备侧出现句柄压力（漏检/ANR/`AccessibilityNodeInfo` 相关日志），此条重开，改法=出队上限之外再补一次"队列余货统一回收"，属取数侧、不动判据。 |

## S31 批四裁（老板 2026-09-24 第四批，AskUserQuestion 原文照录，一问一答不留解释空间）

| # | 议题 | 裁决（老板答语原文） | 落点与代价 |
|---|---|---|---|
| S31-B1 | `ANYTOUCH-S31-ORDER.md` §3 那道"待老板一句话"的门：31-B（A1/A2/A3，住 `executor/NodeTaskRunner.kt`）放不放 | **"放行，立刻派单"** | 派单书 `orders/ANYTOUCH-S31-B-DISPATCH.md`；判据靶表沿用 §1 的 A1/A2/A3 原文不改。§3 原注的代价照实兑现：动了派发/复核边，"没改坏"这句话必须由**模拟器全量 + K40（K80 在位则同批）复验**才配得上，设备关由主窗跑并记在主窗头上 |
| S31-B2 | 待办 14：编译在跑时「执行任务」与「步骤编辑」两入口要不要并入 S3-R4-1 那条互斥 | **"并进来（主窗倾向）"** | 裁决面从"开录/停止"两入口扩到四入口（开录/停止并编译/执行任务/步骤编辑）。真实后果若不并：编译归来整本替换步序账，用户期间改的那一步**被无声覆盖**（不报错）。落点=S3-F 批，`evidence/S3/slice-e0-compile-mutex.md` §7-2 是登记处 |
| S31-B3 | 待办 15：编译器放行的动作词表 **宽于** 执行器支持集（"编得出、跑不动"，两侧从未对拍）怎么裁 | **"收紧词表（编译侧拒并显式）**" | 落点=S3-F 批：`acceptModelActions` 对不支持类型当场拒并上屏拒因（错误必显示），提示词明确要求 click/scroll/type/wait 集。**硬约束**：收紧后的词表**必须从单一真源取**（执行器支持集或 `core/` 契约枚举），不许在编译侧再抄一份字面量——两处词表正是这条雷的成因（雷 18 同族）。副作用照报：模型偶交 key/back 会变成"一次拒 + 重编"，命中率阈值仍待老板定（ORDER §4-3） |
| S31-B4 | K40 不在 adb 上，切片 E 真机最终全绿轮与 E11（`E_WIPE=1` 双槽清除，需人在真机点一下）要不要现在接机 | **"这就去连 K40"** | 主窗按 §3 顺序先收 E：真机 keyed 轮 raw 逐字追加 `evidence/S3/raw/byok-smoke-k40-e1pre.log` 新段 `[K4]`，达标后 STATUS 待办 12 转正、METRICS 记断言数，再落 E 收口 commit；31-B 的设备复验排在 E 之后，两批不抢同一台机 |

## S31-B 批内主窗自裁两条（第一条源自 worker 与母单字面的冲突，老板可覆，2026-09-24）

| # | 议题 | 裁决 | 依据与代价 |
|---|---|---|---|
| S31-B5 | 母单 `ANYTOUCH-S31-ORDER.md` §1 A1 那一行的"下沉缝"字面签名是 `redispatchPlan(performed: Boolean, type: ActionType, attempt: Int)`——**这个签名在磁盘上写不出来** | **采纳 `type: String`**，其余三档结论（`Skip`/`RetryOnce`/`GiveUp`）与"封顶恰好一次"照母单字面不动 | 依据：`core/contracts/src/main/kotlin/com/anytouch/contracts/Constants.kt:21-30` 的 `ActionType` 是 `object` + `const val String`（常量在 :22-29），注释自己写着"**非严格枚举，留扩展位**"（:19），`Action.type` 在契约里就是 `String`——把它当类型写进签名要么改冻结的 `core/`（§2-3 禁止），要么造一个假的枚举镜像（两处词表，雷 18 同族）。比较仍写 `type == ActionType.WAIT`，判据语义与原字面 `action.type != WAIT` 逐字同。代价照实：母单那张表的"字面签名"从此与代码有一格差异，**由本条裁决承担**，worker 须在自述 §5 登记偏差；若老板要"真类型"，那是**改 `core/` 契约面**的另一次放行，不在 31-B 范围内 |
| S31-B6 | 31-B 第一次派工**中断**（worker 侧模型服务连接被切断，非交付未过）：跑到 16 次工具调用、约 10 分钟，工作树只留下一个未接线的新文件 `app/.../executor/ExecutorDecisions.kt`（113 行），`NodeTaskRunner.kt` **一行未改** | **保留该文件为续跑基座，重派一个 worker 接着做**，不从头再来 | 主窗逐条对盘核过这份半成品与原判据：① A3 的 `want.isBlank()` 早退 == 原码 `val want = input.trim()` + `if (want.isNotEmpty())`（`NodeTaskRunner.kt:445,451`，trim 后判空即"空白即不构成凭据"，等价）；② 兜底扫树仍是 `className` 含 `EditText` **且** `text` 含输入、文档序 `firstOrNull`（:456-458），匹配键是输入不是线索（注释也照写）；③ A2 `setTextOk || pasteText` 合计语义与两处派发边（:305、:329）一致，且**短路留在平台侧**的理由写清了（把"何时真去调 PASTE"表达进纯函数=要求无条件派发，反而是语义变化）；④ A1 三档与"仍 false 才收 `perform_failed`"一致。**它没做的**：平台侧转调接线、JVM 用例、四关自检——所以这不是"已交付被打回"，METRICS 记"中断重派"不记返工也不记虚报。诚实边界：A3 的"句柄活读优先"那半边仍在平台侧（那是取数），worker 若把它当成判据搬走=错，续跑时不许动这条 |

## S31-B 批内主窗自裁三条（源自 worker 交付自述 §5 的三处待裁事实，老板可覆，2026-09-24）

| # | 议题 | 裁决 | 依据与代价 |
|---|---|---|---|
| S31-B7-D1 | A1 把两个 `if` 改写成 `while(true){when(redispatchPlan)}`，唯一分叉输入是"WAIT 抵达 `runNodeStep`"：基线会收 `perform_failed`，新码走 Skip→成功 | **接受，不动代码**，等价性由测锁死 | 依据：`runNodeStep` 全仓只有一个调用点（`NodeTaskRunner.kt:109`），WAIT 在 `run` 里走 `delay` 分支、从不派发 ⇒ 该分叉在**可达输入集上为空**（与 S31-B6 已钉的 A1 等价性一致）。worker 自行复核此点并新增队列级锁 `wait步在任何设备拒答下也绝不产perform_failed`（`NodeTaskRunnerTest` 25→26）把它钉成回归防线，不是拿注释顶。代价：若将来给 WAIT 补执行档（S31-B3 那条未被选的路），需同步重判这条边 |
| S31-B7-D2 | A3 落地为纯函数后，`landedViaTreeScan` 的 `want.isBlank()` 早退在**函数内部**，但平台侧仍每次先 `device.root()?.preOrder()?.map{LandedNodeFact}` 摊平整棵树再传入——空白 want 且句柄活读全程返回 null 时，一圈一次整树摊平、最多 ~17 圈（`landedTimeoutMs 4000 / locatePollMs 250`）才被内部早退丢弃；基线的 `if (want.isNotEmpty())` 本可整段跳过 | **不改，登记为诚实边界** | 依据三条：① 判据结论逐字不变（`firstOrNull` 命中的文档序合格节点同一个；空白 want 两侧同返 null），改的只是取数次数不是语义；② 可达性双窄——须"空/纯空白 input 的 type_text 步" **且** "句柄活读每圈 null"，而基线 `text?.contains(want)` 在 want="" 时恒真、句柄只要读出任意文本第一圈即 return，只有死句柄才跑满 17 圈；③ 上限由 `landedTimeoutMs` + 每圈圈首 `killSwitch.isStopped()`（≤250 ms 出环）兜住，且读的是无障碍树快照、无设备往返。两个"顺手修"都被否：平台侧再写一句 `if (want.isBlank()) 跳过` = 把 A3 判据镜像回平台层（雷 18 同族，正是本批要消灭的形态）；把形参改成 `Sequence`/`() -> List` = 越出母单 §1 钉死的 `List<LandedNodeFact>` 字面签名，超出 31-B 范围。**重开条件**：设备侧若出现句柄/内存压力（漏检、ANR、`AccessibilityNodeInfo` 相关日志），把缝改成惰性需另立一次放行。 |
| S31-B7-D3 | `want` 在平台 `awaitLanded :465` 与纯函数 `landedViaTreeScan :108` 各 `trim()` 一次 | **非问题**（幂等，不构成"两处实现"） | 内层 trim 是母单 §1 A3 反例④硬要求——纯函数必须**自身**扛住未 trim 的 `want`（它是唯一权威），外层 trim 基线即有且其产物还要喂未搬的句柄活读半边（:470）。两次 trim 对任一输入结果同一次 ⇒ 不是镜像。登记于此供逐行 diff 对照。 |

**另更正 S31-B6 叙述中的一处笔误**：B6 写"工作树只留下…`ExecutorDecisions.kt`（113 行）"，`wc -l` 实测 **112 行**（该文件自 worker1 创建起一字节未改，故系主窗当时误数，非文件变动）。worker 交付自述 §7.3 已同步纠正。此处按"引数字必能指到磁盘"的口径如实回补，不改 B6 其余结论。

## S31-B8（主窗验收裁决，09-24 午后）：STAGE-31-B **ACCEPTED**，挂起项由 K40 复验兑付

| 编号 | 事项 | 结论（主窗裁，非老板裁） | 依据与边界 |
|---|---|---|---|
| S31-B8 | 31-B 自 S31-B7 起挂的唯一缺口"K40 device-smoke 复验"（雷12 持久拒模拟器不复现，S31-B1 钉"模拟器全量 + K40 复验才配得上'没改坏'"） | **ACCEPTED**。判据没拿模拟器口径冒用真机：按归档真机 profile 口径（`t3-k40-first-contact.md`，R3 裁"真机不适配模拟器脚本口径"）三条判——① 红项 ⊆ 09-22 首跑已知名单且形态逐字同首轮（新红=零）② 派发/复核关键链真机绿（C3 真机 type_text 成功、C4 负例、C8b 解绑重绑自愈）③ 零假绿（C2/C5/C7 的 MIUI 持久拒**每一格都显式说话**：EXECUTOR_ERROR+节点路径+步级 ok/total 计数，双通道在真拒答下不冒成功——这正是必须上真机才能锁的那半边）。#38 的 A1/A2/A3 随此清零。 | 逐条对表与前置（含"旧 apk mtime 早于最后一处源码改动→强制重出包 md5 `37d2e199` 才装机"一条过程事实）见 `evidence/S31/stage31-b-k40-reverify.md`；raw `[K5b]` 段带时间戳入 `evidence/S3/raw/byok-smoke-k40-e1pre.log`（二进制追加、前缀哈希自证旧字节零改动）。边界如实记：**K80 不在位**（"在位则同批"括注未触发，非漏跑）；C5/C7 确认链本轮被上游 SET_TEXT 拒答截断没重走到，其真机证据仍由 T3 历史轮 + C7x 洁净锁承担，本裁不冒领也不撤销。同轮 byok-smoke `[K5]` 44 断言无失败（含 E11a~d，老板亲点清除，切片 E 由此收口，见 `slice-e2-k40-key.md` §6）。

## S4 批裁决（老板 2026-09-24 午后令原文；同一指令发了两遍、内容逐字一致，按一份入账）

| # | 议题 | 裁决（老板答语原文） | 落点与代价 |
|---|---|---|---|
| S4-R6 | ORDER §4-3 留的口子："命中率统计需要 ≥10 条真实意图的矩阵，成功率口径留待老板定样本集与阈值"；另 S31-B3 副作用登记时再次点名"命中率阈值仍待老板定" | **"命中率阈值定 70%：三个模型里至少 2 个输出一致就算编译通过，跟 AI 众议院的共识逻辑一致，不卡太死"** | 本窗读法（如实标注，老板可覆）：**单条意图的通过判据=三模型输出一致（共识档），整批矩阵的过线阈值=70%**。两处硬事实不装作已解决：① 当前编译面是**单次调用单 BYOK 端点**，"三个模型"要么三把 Key/三供应商、要么一把能跨模型的 Key——供应商侧可行性与 token 成本（×3）在 S4 工单待裁项列清，**本批不擅自动编译架构**；② "输出一致"的对齐口径（步序逐字等？语义等？）必须落成可测判据才能写码，先立反例锁再谈实现。阈值 70% 即刻生效为**验收线**：矩阵未搭出来之前，任何人声称"命中率达标"都无凭据。 |
| S4-R7 | STATUS 待办 13②：整页要滚三屏才看完，"录制→停止并编译"手指路径怎么排 | **"三屏排布先不动，现在能用就行，后面实际用着不顺手再调"** | UI 排布维持现状（`verticalScroll` Column + BYOK 面板），本窗**不自发改排布**；E1 那组"折叠线以下滚得到"设备断言（E1j/E1k）继续当回归防线。**待办 13② 关闭**；"后面不顺手再调"=调权留在老板，届时另开单。 |
| S4-R8 | S4 开不开、开什么 | **"直接开 S4：先打个 debug APK 出来，你装 K40 上自己跑全流程，同时写装机引导话术，准备后续 Gumroad 上架"** | 开单 `orders/ANYTOUCH-S4-packaging-ORDER.md`：**S4-a**=HEAD 出 debug APK→装 K40→测试通道自跑全流程（授权/填 Key 走 S3-R5 代填通道〔E11 后机上凭据已真清，不找老板的手〕→意图→真 HTTPS 编译→执行），真请求按本令授权打（次数在工单里钉上限）；**S4-b**=装机引导话术（**英文**、Gumroad 上架预备——§4-2"现有 UI 全中文 vs 海外专供"的冲突面随本批兑现，APP 内文案是否 localization 另裁）。纪律不变：Key 字面量不入 git/日志/raw，屏上只允许尾 4 位形态。 |
