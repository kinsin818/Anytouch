# 录制 UI 视觉实证探针（同构建 a027640，emulator-5554 / API 35，设备时钟 07:28–07:29，app pid 12531 全程未换）
# 目的：把"红字话术会不会过期""步序账与任务框会不会脱钩"两条从推理升级为实证。
# 注：logcat 缓冲会滚动作废，本文件是当时逐字截取的片段；截图 A/B/C 与本文行号一一对应。

## T0 之前（屏上状态 = img/recui-probe-A-idle-stale-red.png，07:28）
# 状态行：无障碍服务：已连接 / 空闲 / 未在录制
# 红字①（startRejection）：任务执行中不能开录：执行器的手会被录成你的意图。请等本轮结束（或点悬浮球停止）后再录。
# 红字②（editRejection）：当前没有可编辑的步骤：请先录制并点「停止并编译」，有步骤后才能增删改。
# 步序账：步骤 0；任务框文本：[{"action_id":"rec-0003",...Bluetooth...}]（单步）
# 采集方式：adb shell screencap -p /sdcard/probe-before.png（未用 uiautomator dump ⇒ 不打无障碍绑定，无需 wait_service_bound）

## T1 注入开录（测试通道，与按钮同写口）
$ adb shell "am start -f 536870912 -n com.anytouch.app/.MainActivity --es record_start com.android.settings"
I/AnytouchRun(12531): S2SMOKE record start target=com.android.settings
# 关键：这一行附近无 `record start refused gate=` ⇒ 门禁在"红字①仍挂屏"的同一刻判的是 READY 并开录成功。
# 截图 img/recui-probe-B-recording-accepted.png（07:29）：球变红「停录」= 会话确已在录，红字①同时消失。
# ⇒ 定性：startRejection 只有"下一次开录（成功或失败）"这一条覆盖边，没有"状态已变即作废"的过期边；
#   空闲态屏上挂着"任务执行中不能开录"是**误报**（推理与实证同向：真点开始录制会被放行）。

## T2 停止并编译（本轮无确认点击 ⇒ 空产物）
$ adb shell "am start -f 536870912 -n com.anytouch.app/.MainActivity --ez record_stop true"
I/AnytouchRun(12531): S2SMOKE compiled ok actions=0 drops=0 merged=0 events=2 target=com.android.settings
W/AnytouchRun(12531): S2SMOKE compiled EMPTY task, 步序账清零、不写回放建议（丢弃归因见 DETAIL）
# 截图 img/recui-probe-C-orphan-task-json.png：步序账"步骤 0 + 步序账为空"，
# 但任务框仍是 T0 那份 rec-0003 单步 JSON，"执行任务"按钮可点 ⇒ 屏上同时存在"账=0 步"与"可回放的 1 步任务"。
# ⇒ 定性：这是"不夺字"（§4 第 5 条）的反面代价——框内文本与步序账在清零后彻底脱钩，成为账外的孤儿任务。

## 附：本探针不采信的同 pid 残留行（上一轮测试通道行为，时间戳已不可对号）
I/AnytouchRun(12531): S2SMOKE record inject accepted events=4 target=com.android.settings
I/AnytouchRun(12531): S2SMOKE step edit ok=remove index=0 before=2 after=1
W/AnytouchRun(12531): S2SMOKE step edit ok=remove 步序账清零，不写回放建议（无步骤可放）
W/AnytouchRun(12531): S2SMOKE step edit refused gate=EMPTY_LEDGER op=remove index=0 ledger=0 detail=当前没有可编辑的步骤：请先录制并点「停止并编译」，有步骤后才能增删改。
# 说明：这几条与 T1 之间无法用时间戳排序（当时用 `-v brief` 未带时间），故只作背景，不参与本探针断言。
# 教训入规：探针类取证一律 `-v time` 起停各打一次标记，否则"紧邻两行"不等于"前后两行"。
